<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            margin: 0;
            padding: 0;
        }

        .news-card {
            border: 1px solid #ddd;
            padding: 15px;
            margin: 10px;
            display: flex;
            flex-direction: column;
            align-items: center;
            height: 100%; /* Let the height expand based on content */
            box-sizing: border-box;
            text-align: center; /* Center-align text content */
            background-color: #ffffff; /* Add a white background */
            box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1); /* Add a subtle shadow */
        }

        .news-card img {
            max-height: 150px;
            width: 100%;
            object-fit: cover;
            margin-bottom: 10px;
        }

        .news-card h5 {
            font-size: 1.2rem;
            margin: 10px 0;
            display: -webkit-box;
            -webkit-line-clamp: 2;
            -webkit-box-orient: vertical;
            overflow: hidden;
        }

        .news-card p {
            font-size: 0.9rem;
            margin-bottom: 10px;
            flex-grow: 1;
            display: -webkit-box;
            -webkit-line-clamp: 3;
            -webkit-box-orient: vertical;
            overflow: hidden;
        }

        .news-card .btn-primary {
            margin-top: auto;
        }

        .profile-icon {
            max-height: 30px;
            cursor: pointer;
            margin-left: 10px;
        }

        body {
            margin: 0;
            padding: 0;
        }

        .news-card:hover {
            transform: scale(1.05);
            /* Increase the size slightly on hover */
            transition: transform 0.3s ease;
            /* Apply a smooth transition effect */
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.3);
            /* Add a subtle shadow */
            animation: blink 1s infinite;
        }

        @keyframes blink {
            0%, 100% {
                opacity: 1;
            }
            50% {
                opacity: 0.8;
            }
        }

        .dark-mode {
            background-color: #121212;
            color: #ffffff;
        }

        .light-mode {
            background-color: #ffffff;
            color: #000000;
        }

        .dark-mode-btn,
        .light-mode-btn {
            cursor: pointer;
            margin-right: 10px;
        }

        .dark-mode .news-card {
    background-color: #333; /* Darker background color for dark mode */
    color: #fff; /* Light text color for dark mode */
    border-color: #555; /* Darker border color for dark mode */
}

.dark-mode .news-card img {
    filter: brightness(70%); /* Adjust image brightness in dark mode */
}

        .container {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            /* Ensure the container takes up at least the full viewport height */
        }

        .row {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            margin: 0 -10px;
            /* Remove default margin and add negative margin to compensate for column margins */
        }

        /* Style for the categories button and line */
        .categories-line {
            width: 100%;
            border-top: 1px solid #ddd;
            margin: 20px 0;
        }

        .categories-dropdown {
            display: flex;
            justify-content: center;
            margin-bottom: 20px;
        }

        .categories-button {
            background-color: #fff;
            color: #000;
            border: 1px solid #ddd;
            padding: 5px 10px;
            border-radius: 5px;
            cursor: pointer;
        }

        .navbar-custom .nav-link {
            color: #ffffff;
            font-weight: bold;
            transition: color 0.3s ease;
        }

        .navbar-custom .nav-link:hover {
            color: #ffc107;
        }

        .navbar-custom .navbar-brand {
            font-size: 1.5rem;
            color: #ffc107;
            font-weight: bold;
        }
    </style>
</head>

<body>
   <nav class="navbar navbar-expand-lg navbar-dark bg-dark navbar-custom">
        <div class="container-fluid">
            <a class="navbar-brand" href="#">Express Dispatch</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent"
                aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                        <a class="nav-link" href="/general">General</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/business">Business</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/sports">Sports</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/technology">Technology</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/entertainment">Entertainment</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/tamilnews">Tamil News</a>
                    </li>
                </ul>
                <div>
                    <span class="dark-mode-btn">🌙</span>
                    <span class="light-mode-btn">☀️</span>
                </div>
                <form class="d-flex">
                    <input class="form-control me-2" type="text" id="newsQuery" placeholder="Search news">
                    <button class="btn btn-outline-warning" type="button" id="searchBtn">Search</button>
                    <!-- Add the profile icon here -->
                    <img src="https://tse1.mm.bing.net/th?id=OIP.cxcqa9RvhGnRd8x3N2oBdgHaHn&pid=Api&P=0&h=180"
                        alt="Profile" class="profile-icon" id="profileIcon">
                </form>
            </div>
        </div>
    </nav>

    <div class="container mt-4">
    <div class="row">
        <div class="col-md-12">
            <div class="categories-line"></div>
            <div class="categories-dropdown text-center">
                <div class="dropdown">
                    <button class="categories-button btn btn-secondary dropdown-toggle" type="button" id="categoryDropdown" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Categories
                    </button>
                    <div class="dropdown-menu" aria-labelledby="categoryDropdown">
                        <a class="dropdown-item" href="/general">General</a>
                        <a class="dropdown-item" href="/business">Business</a>
                        <a class="dropdown-item" href="/sports">Sports</a>
                        <a class="dropdown-item" href="/technology">Technology</a>
                        <a class="dropdown-item" href="/entertainment">Entertainment</a>
                        <a class="dropdown-item" href="/tamilnews">Tamil News</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="row" id="newsGrid"></div>
</div>
    <script>
    const API_KEY = "adf65f375c5f4f0fa2cd81eb83189047";

    const newsGrid = document.getElementById("newsGrid");
    const searchBtn = document.getElementById("searchBtn");
    const newsQuery = document.getElementById("newsQuery");

    async function fetchNews(query) {
        try {
            // Change this line inside the fetchNews function
            const url = query ? `https://newsapi.org/v2/everything?q=${encodeURIComponent(query)}&apiKey=${API_KEY}` : `https://newsapi.org/v2/top-headlines?country=in&category=technology&apiKey=${API_KEY}`;

            const response = await fetch(url);
            const data = await response.json();

            if (response.ok) {  
                const articles = data.articles;

                newsGrid.innerHTML = "";

                articles.forEach(article => {
                    const newsCard = document.createElement("div");
                    newsCard.classList.add("col-md-3", "news-card");
                    newsCard.innerHTML = `
                        <img src="${article.urlToImage}" alt="${article.title}" style="max-height: 200px;">
                        <h5 class="mt-2">${article.title}</h5>
                        <p>${article.description}</p>
                        <a href="${article.url}" target="_blank" class="btn btn-primary">Read more</a>
                    `;

                    newsGrid.appendChild(newsCard);
                });
            } else {
                console.error("Error fetching news:", data.message);
            }
        } catch (error) {
            console.error("Error fetching news:", error);
        }
    }

    searchBtn.addEventListener("click", () => {
        const query = newsQuery.value.trim();
        fetchNews(query);
    });

    // Initial fetch when the page loads
    fetchNews("technology");

    // Profile icon click event (redirect to profile page)
    const profileIcon = document.getElementById("profileIcon");
    profileIcon.addEventListener("click", () => {
        // Redirect to user profile page
        window.location.href = "/profile"; // Replace with your actual profile page URL
    });
    const darkModeBtn = document.querySelector(".dark-mode-btn");
const lightModeBtn = document.querySelector(".light-mode-btn");
const body = document.body;

darkModeBtn.addEventListener("click", () => {
    body.classList.add("dark-mode");
    body.classList.remove("light-mode");
});

lightModeBtn.addEventListener("click", () => {
    body.classList.remove("dark-mode");
    body.classList.add("light-mode");
});


</script>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
